<nav id="navigation" class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header shop-name">
      <%= link_to 'LiveSaverSoft Store', root_path %>
    </div>
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <li><%= link_to t('devise.links.sign_out'), destroy_user_session_path, method: :delete, class: "auth-button" %></li>
        <% else %>
          <li><%= link_to t('devise.links.sign_in'), new_user_session_path, class: "auth-button" %></li>
          <li><%= link_to t('devise.links.sign_up'), new_user_registration_path, class: "auth-button sign-up fa fa-pencil" %></li>
      <% end  %>
        <% if current_order %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= t('theme.header.lang') %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('language.en'), { locale: 'en' }, class: 'en' %>
              </li>
              <li>
                <%= link_to t('language.ua'), { locale: 'ua' }, class: 'ua' %>
              </li>
            </ul>
          </li>
          <%= render 'layouts/basket' %>
        </ul>
      <% end %>
  </div>
</nav>
<div class="nav-wrap"></div>
