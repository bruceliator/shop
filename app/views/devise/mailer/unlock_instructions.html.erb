<p><%= t('mailer.hello_message') + " #{@resource.email}" %>!</p>

<p><%= t('mailer.unlock_instructions.subject') %></p>

<p><%= t('mailer.unlock_instructions.click_link') %></p>

<p><%= link_to t('mailer.unlock_instructions.unlock'), unlock_url(@resource, unlock_token: @token) %></p>
