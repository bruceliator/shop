<h2><%= @product.name %></h2>

<% if @product.default_image %>
  <%= image_tag @product.default_image.path, :width => '200px', :style => "float:right" %>
<% end %>

<p><%= @product.short_description %></p>
<p>
  <b><%= number_to_currency @product.price %></b>
  <%= link_to t('buttons.add_to_basket'), product_path(@product.permalink), class: 'btn btn-success', :method => :post %>
</p>

<hr>
<%= simple_format @product.description %>
<hr>

<p><%= link_to t('buttons.back'), root_path, class: 'btn btn-default' %></p>
